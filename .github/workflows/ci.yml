name: ci
on:
  push:
    branches: [master]

jobs:
  scenarios:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - run: make test
      - if: failure()
        run: docker compose logs
